{
  "kind": "build_request",
  "title": "Add fixed PIX QR code to payment checkout",
  "projectName": "Rotas e Direitos",
  "priority": "normal",
  "requirements": [
    {
      "id": "REQ-1",
      "text": "Generate a fixed PIX QR code in the payment checkout flow using the email key proj.defdriver+pagbank@gmail.com",
      "target": "frontend",
      "source": {
        "messageIds": [
          "msg-last"
        ],
        "quotes": [
          "no checkout de pagamento crie um qr code pix fixo com a chave email: proj.defdriver+pagbank@gmail.com"
        ]
      },
      "acceptanceCriteria": [
        "PIX QR code is displayed in the checkout page",
        "QR code uses the fixed PIX key proj.defdriver+pagbank@gmail.com",
        "QR code is scannable and follows PIX QR code standards",
        "The PIX key email is visible alongside the QR code for manual entry"
      ]
    },
    {
      "id": "REQ-2",
      "text": "Implement PIX payload generation logic in frontend/src/lib/payments/pixPayload.ts to create valid PIX QR code strings",
      "target": "frontend",
      "source": {
        "messageIds": [
          "msg-last"
        ]
      },
      "acceptanceCriteria": [
        "Function generates valid PIX EMV QR code payload",
        "Payload includes the email PIX key proj.defdriver+pagbank@gmail.com",
        "Payload can optionally include transaction amount and description",
        "Generated string is compatible with QR code libraries"
      ]
    },
    {
      "id": "REQ-3",
      "text": "Integrate QR code display component in CheckoutPage.tsx to render the PIX QR code when PIX payment method is selected",
      "target": "frontend",
      "source": {
        "messageIds": [
          "msg-last"
        ]
      },
      "acceptanceCriteria": [
        "QR code appears in the checkout flow",
        "Component displays both the scannable QR code and the PIX key text",
        "UI provides clear instructions for PIX payment completion",
        "Layout is responsive and works on mobile devices"
      ]
    }
  ],
  "constraints": [
    "Use the exact PIX key email: proj.defdriver+pagbank@gmail.com",
    "QR code must be a fixed/static code, not dynamically generated per transaction",
    "Implementation must be frontend-only; no backend changes required"
  ],
  "nonGoals": [
    "Dynamic PIX keys or per-transaction QR codes",
    "Backend payment verification or webhook integration",
    "Integration with payment gateway APIs for PIX",
    "Automatic payment confirmation or status tracking"
  ],
  "imageRequirements": {
    "required": [],
    "edits": []
  }
}